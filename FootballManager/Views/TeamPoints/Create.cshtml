@model FootballManager.Models.TeamPoints

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>


@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>TeamPoints</h4>
        <hr />
        @Html.ValidationSummary(true)

        <div class="form-group">
            @Html.LabelFor(model => model.TeamID, "TeamID", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("TeamID", String.Empty)
                @Html.ValidationMessageFor(model => model.TeamID)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.ChampshipID, "ChampshipID", new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.DropDownList("ChampshipID", String.Empty)
                @Html.ValidationMessageFor(model => model.ChampshipID)
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Points, new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Points)
                @Html.ValidationMessageFor(model => model.Points)
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
